import 'package:flutter/material.dart';

/// Uygulama renkleri
class AppColors {
  // Ana renkler
  static const primary = Color(0xFF6200EA);
  static const primaryLight = Color(0xFF9E47FF);
  static const primaryDark = Color(0xFF0400B6);
  static const secondary = Color(0xFF03DAC6);
  static const secondaryLight = Color(0xFF66FFF8);
  static const secondaryDark = Color(0xFF00A896);

  // Metin renkleri
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const textTertiary = Color(0xFFBDBDBD);

  // Durum renkleri
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFE53935);
  static const warning = Color(0xFFFFB74D);
  static const info = Color(0xFF2196F3);

  // Arka plan renkleri
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);
  static const cardBackground = Color(0xFFFFFFFF);

  // Tema arka plan renkleri
  static const lightBackground = Color(0xFFF5F5F5);
  static const darkBackground = Color(0xFF121212);

  // Bilgi seviyesi renkleri
  static const beginnerColor = Color(0xFF4CAF50); // Yeşil
  static const intermediateColor = Color(0xFF2196F3); // Mavi
  static const expertColor = Color(0xFFE91E63); // Pembe
}
